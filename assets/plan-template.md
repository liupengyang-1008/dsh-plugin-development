<!--
决策留痕模板 —— 复制到你的插件项目里的 docs/plan.md，随开发进度勾选。

它记录的是「你当时为什么这么选」，不是「构建结果」。它的用途是回放与交接：
  · 换个会话 / 换个人接手时，不用重新推理一遍形态与模板选择
  · 出问题时能看出是「选型错了」还是「实现错了」——这是回退到哪一步的依据
  · 报告完成前，能一眼看出哪些验证其实跳过了

⚠️ 它**不是门禁依据**。打了勾不代表插件正确；正确性只由真实组合里的冒烟与验收证明。
-->

# {插件名} · 插件开发计划

> 形态：`{函数式插件 | 服务插件 | 组合包}` · 模板：`{T1..T12}` · 基线 DSH 版本：`{版本/commit}`
> 目标 profile：`{profile 名}` · 分发方式：`{git 源 | 本地目录 | npm}`

## 第 0 步 · 版本闸门（强制）

- [ ] 目标 DSH 版本已确认：`{版本 / commit}`
- [ ] 三问答完（对哪个版本写 / API 属 S-M-V 哪级 / 能不能核验）
- [ ] 探针已跑：`python <skill>/scripts/dsh-api-probe.py <仓库>` → 退出码 `{0|1|2|3}`
- [ ] 未核验的 M/V 级 API（如有，逐条列出，禁止写成肯定句）：
  - `{符号名} —— 未对当前版本核验`

> 探针退出码 1 = 有 STALE，**不要照抄**；3 = 断言表为空，结果无效；2 = 仓库路径不对。

## 第 1 步 · 选型

- [ ] 形态判定：{函数式 / 服务 / 组合包}　理由：{一句话}
- [ ] 已确认**没有 `export default`**（函数式插件）
- [ ] 模板编号：`{T…}`　路由依据：{用户在需求里怎么说的}

## 第 2 步 · 环境

- [ ] `--dump-config` 能打出组合树
- [ ] `DSH_HOME` / profile 目录已定位：`{路径}`
- [ ] 环境降级情况：{无 / 无 Node·pnpm —— 已改为产出等价文件}

## 第 3 步 · 最小闭环

- [ ] 空插件能加载，且**在真 profile 里有可见结果**
- [ ] 可见结果是什么：`{…}`

## 第 4 步 · 冒烟

- [ ] 三件套就位：命令 `{}` / 真实调用 `{}` / 可见标记 `{}`
- [ ] 逐项对照判定表通过（组合包 · 命令 · 工具 · 服务 · 事件 · HTTP · UI · 配置 —— 取实际用到的）

## 第 5 步 · 写功能

- [ ] `inject` 覆盖所有用到的服务
- [ ] 可选服务用 `ctx.get('x')`（不是 `ctx.x`）
- [ ] 需清理的副作用在 `ctx.effect()` 内并返回 disposer

## 第 6 步 · UI（如涉及）

- [ ] 两个半侧 + 三个登记点齐全
- [ ] 界面出现预期内容，控制台无 `slot entry crashed`

## 第 7 步 · 调试

- [ ] 无 PENDING 条目（`--dump-config` + 启动日志）
- [ ] 无静默失败：失败路径能给指向原因的报错

## 第 8 步 · 打包

- [ ] 合同不变量 5 条全过（含名称三处一致）
- [ ] `files` 清单齐全；`lib/` **未**手改

## 第 9 步 · 安装验证

- [ ] `dsh plugin --profile {名} add {包}` 成功
- [ ] **从最终分发源重装**后冒烟仍通过

## 第 10 步 · 工程化 / 发布

- [ ] 命名、README 强制节、测试、门禁、提交规范已按 `references/07-conventions.md` 过一遍
- [ ] 发布物与真实安装 ref 一致（README 里的 `<owner>` 等占位已替换）

## 决策变更记录（回退到第 1~2 步时在此追加）

> 回退原因分两类：**合同级 / 形态级错误** → 回第 1~2 步重选；**业务级错误** → 留在第 5~7 步原地修。
> 每次回退写一行：`{日期} 从 {原形态/模板} 改为 {新形态/模板} —— 原因：{…}`

## 跳过的验证（如实记录，不许写成「通过」）

| 跳过的项 | 原因 | 替代证据（如有） |
|---|---|---|
| `{…}` | `{环境无 pnpm / 版本未核验 / …}` | `{…}` |
